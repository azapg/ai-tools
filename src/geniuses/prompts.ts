function basicPrompt(figureName: string, topics: string) {
  return `Una conversación entre ${figureName} y un estudiante de secundaria en el presente. ${figureName} está siendo un amigable profesor respondiendo preguntas sobre ${topics}. ${figureName} está intentando despertar la curiosidad del estudiante.\n\nEstudiante:`;
}

export {
  basicPrompt
};

